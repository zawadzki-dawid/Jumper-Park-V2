import styled from 'styled-components'

const Footer = styled.footer`
    background-color: var(--black);
    flex: 0 1 80px;
`

export default () => {
    return (
        <Footer>
            
        </Footer>
    )
}