import Loader from './Loader'

export default {
    args: {
        visible: true
    },
    title: 'Components/Loader'
}

// Default

export const Visible = () => {
    return (
        <Loader>
            <div>
                
            </div>
        </Loader>
    )
}